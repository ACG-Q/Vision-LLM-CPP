name: 🛠️ Multi-Platform Builder

on:
  workflow_dispatch:
    inputs:
      build_type:
        description: '编译模式'
        required: true
        default: 'Release'
        type: choice
        options: [Release, Debug]
      paddle_version:
        description: 'Paddle 预测库版本'
        default: '2.5.0'

jobs:
  # --- Android 编译任务 ---
  build-android:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: 🛠️ Setup NDK
        uses: nttld/setup-ndk@v1
        id: setup-ndk
        with:
          ndk-version: r21e
          add-to-path: true

      - name: 📦 Build Android (arm64-v8a)
        env:
          ANDROID_NDK_HOME: ${{ steps.setup-ndk.outputs.ndk-path }}
        run: |
          chmod +x ./scripts/build_android.sh
          ./scripts/build_android.sh ${{ github.event.inputs.build_type }}

      - name: 📦 Collect Android Artifacts
        run: |
          mkdir -p output_android
          cp build_android/libocr_lib.so output_android/
          cp inference_lite_lib/cxx/lib/libpaddle_light_api_shared.so output_android/

      - name: 📤 Upload Android Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: ocr-android-lib
          path: output_android/*

  # --- Windows 编译任务 ---
  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: 🛠️ Setup MSVC
        uses: ilammy/msvc-dev-cmd@v1

      - name: 📦 Build Windows (x64)
        shell: powershell
        run: |
          ./scripts/build_windows.ps1 -BuildType ${{ github.event.inputs.build_type }}

      - name: 📦 Collect Windows Artifacts
        shell: powershell
        run: |
          New-Item -ItemType Directory -Force -Path output_win
          
          # 1. 复制主库
          Write-Host "Collecting ocr_lib.dll..."
          if (Test-Path "build_win\Release\ocr_lib.dll") {
              Copy-Item "build_win\Release\ocr_lib.dll" output_win\
          } elseif (Test-Path "build_win\Debug\ocr_lib.dll") {
              Copy-Item "build_win\Debug\ocr_lib.dll" output_win\
          }

          # 2. 搜索并复制 Paddle 相关 DLL (包括 paddle_inference.dll 和 MKL 依赖)
          Write-Host "Collecting Paddle dependencies..."
          Get-ChildItem -Path paddle_inference -Filter *.dll -Recurse | ForEach-Object {
              Write-Host "Found: $($_.FullName)"
              Copy-Item $_.FullName output_win\
          }

          # 3. 搜索并复制 OpenCV 运行库 (通常在 bin 目录下)
          Write-Host "Collecting OpenCV dependencies..."
          Get-ChildItem -Path opencv\build -Filter *.dll -Recurse | Where-Object { $_.FullName -like "*\bin\*" } | ForEach-Object {
              Write-Host "Found: $($_.FullName)"
              Copy-Item $_.FullName output_win\
          }

          # 4. 列出最终收集的文件
          Write-Host "Total files collected in output_win:"
          Get-ChildItem output_win | Select-Object Name

      - name: 📤 Upload Windows Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: ocr-windows-dll
          path: output_win/*
